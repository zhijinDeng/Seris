# 飞书集成指南

1. 创建飞书企业应用，配置应用凭证。
2. 创建多维表格，按`data/feishu_bitable_schema.csv`建立字段。
3. 配置自定义机器人，将Webhook写入配置文件。
4. 在Aily中创建质量事件查询、任务更新和复盘提交技能。
5. 使用`scripts/feishu_client.py`封装新增记录和机器人卡片发送。

接口字段以质量事件为中心，不绑定某一条产线。新增工况时只需扩展设备、参数、FMEA和控制计划映射。
